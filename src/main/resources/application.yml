spring:
  application:
    name: todoapp
  datasource:
    url: jdbc:postgresql://localhost:5432/todoapp
    username: vhal
    password: zim
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true